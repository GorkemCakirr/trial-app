# Authentication Flow

1. Users authenticate using AWS SSO credentials through AWS CLI
2. The application displays the user's active AWS profile and session information
3. Users can switch between different AWS profiles as needed

![profiles](/select-profile.png)
